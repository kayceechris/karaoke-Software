@echo off
REM ---- Karaoke LAPTOP agent launcher (Windows) ----
REM Copy this file to "start_agent.bat" and fill in your values.
REM start_agent.bat is git-ignored so your token never gets committed.
cd /d "%~dp0"

REM ===== EDIT THESE TWO LINES after you deploy the cloud =====
set CLOUD_URL=https://your-app.onrender.com
set HOST_TOKEN=paste-your-host-token-here
REM ==========================================================

REM ===== Optional: Cloudflare R2, so cloud guests get real video =====
REM Leave blank to skip — cloud /tablet just won't have video (unchanged).
set R2_ACCOUNT_ID=
set R2_ACCESS_KEY_ID=
set R2_SECRET_ACCESS_KEY=
set R2_BUCKET=
set R2_PUBLIC_BASE_URL=
REM ====================================================================

if not exist ".venv\" (
    echo Creating virtual environment...
    python -m venv .venv
)
call .venv\Scripts\activate.bat
echo Installing/updating dependencies...
python -m pip install -q -r requirements-agent.txt

python agent.py
pause
